import { DrawStyleProps } from './types';

export const coordsStyle: DrawStyleProps = {
  textFill: 'black',
};

export const playerStyle: DrawStyleProps = {
  fill: '#5fd',
};

export const cursorStyle: DrawStyleProps = {
  fill: 'rgba(255, 152, 224, 0.25)',
  stroke: '#f1c',
  lineWidth: 2,
};

export const routePointStyle: DrawStyleProps = {
  fill: 'transparent',
  stroke: '#1cf',
  lineWidth: 2,
};
